7.神经网络算法
使用包的是nnet: 单隐藏层前馈网络
重要的参数有: size（设置隐藏层节点数）， decate（权重衰减参数，防止过拟合）， maxit（最大迭代次数）
分类条件: 分类时需要将因变量转变为因子，且因子水平数必须大于等于2
size和decate的选择: 通过交叉验证来选择最优的size和decate参数
常用的深度学习框架：kefras和torch

8.特征工程与降维（PCA）
使用包的是prcomp，用于标准化和中心化数据
绘图展示使用方差解释率（碎石图）
主要用于主成分聚类分析或建模